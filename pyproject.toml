[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sanctions-intelligence-pipeline"
version = "0.1.0"
description = "Data engineering pipeline for sanctions intelligence"
requires-python = ">=3.13"

[tool.setuptools.packages.find]
where = ["src"]
